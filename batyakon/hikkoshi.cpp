#include<iostream>
#include<vector>
using namespace std;

int main(){
    int c;
    cin >> c;
    vector<vector<int> > vec(n, vector<int>(3))
    for(int i = 0; i < c; i++){
        for(int j = 0; j < 3; j++){
            cin >> vec.at(i).at(j);
        }
    }
    int cnt, max;
    for(int i = 0; i < c; i++){
    int max = *max_element(vec.at(i))
    cnt = i;
    }

}
